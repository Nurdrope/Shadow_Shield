# Shadow_Shield — Disable unnecessary kernel protocols
# Apply: sudo cp disable-protocols.conf /etc/modprobe.d/

install dccp /bin/true
install sctp /bin/true
install rds /bin/true
install tipc /bin/true
